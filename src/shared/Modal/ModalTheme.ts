import { createTheme } from '@mui/material';

export const modalTheme = createTheme({
  palette: {
    success: {
      main: '#3FC173',
    },
    secondary: {
      main: '#BBBBBB',
    },
  },
});
